# 📦 Telegram Price Bot

Телеграм-бот для импорта прайс-листа из Excel в PostgreSQL и поиска товаров —
по тексту и по фото этикетки (OCR). С ролевой моделью доступа,
управлением пользователями через приглашения и журналом изменений.

**Стек:** Python 3.12+ · [aiogram 3](https://docs.aiogram.dev/) · PostgreSQL 16 ·
SQLAlchemy 2 (async) · pandas · openpyxl · EasyOCR · Docker Compose

---

## ✨ Возможности

- 🔍 **Поиск по тексту** — точное / частичное / fuzzy-совпадение по `normalized_model`.
- 📷 **Поиск по фото** — распознавание модели на этикетке через EasyOCR. Если товар
  не найден, бот показывает распознанную модель. Фото хранится во временной папке
  **1 час** и затем автоматически удаляется.
- 📊 **Импорт Excel** — загрузка прайс-листа через Telegram (только `.xlsx`) или из
  командной строки, с извлечением встроенных в ячейки картинок.
- ➕ **Добавление товара** — пошагово: фото → модель → описание → цена
  (описание необязательно, можно «Пропустить»).
- ✏️ **Обновление товара** — поиск позиции и обновление фото / модели / описания / цены.
- 🗑 **Удаление товара** — поиск и удаление с подтверждением.
- 👮 **Роли и доступ** — три роли (`SUPER_ADMIN` / `ADMIN` / `USER`), проверки прав до
  бизнес-логики.
- 👤 **Управление пользователями** — супер-админ добавляет пользователей/админов
  одноразовой ссылкой-приглашением и удаляет их из бота.
- 🧾 **Журнал изменений** — `audit_log` фиксирует изменения каталога (добавление,
  обновление, удаление, импорт) и отказы в доступе.

---

## 🗂️ Структура проекта

```text
.
├── src/                          # весь исходный код
│   ├── bot.py                    # точка входа: бот, роутинг, фоновые задачи
│   ├── db.py                     # async engine/session, init схемы, таймзона
│   ├── models.py                 # модели Product, User, AuditLog, Invite
│   ├── keyboards.py              # клавиатуры (компактные, по ролям)
│   ├── import_excel.py           # импорт прайс-листа из Excel в PostgreSQL
│   ├── middlewares/
│   │   └── access.py             #   регистрация пользователя + инъекция роли
│   ├── handlers/                 # обработчики Telegram-сообщений
│   │   ├── search.py             #   поиск товаров
│   │   ├── add_product.py        #   добавление: фото→модель→описание→цена
│   │   ├── update_product.py     #   обновление полей товара
│   │   ├── delete_product.py     #   удаление товара
│   │   ├── add_user.py           #   приглашения (deep-link) и выдача роли
│   │   └── delete_user.py        #   удаление пользователей/админов
│   ├── services/                 # бизнес-логика
│   │   ├── product_service.py    #   поиск exact / partial / fuzzy, CRUD
│   │   ├── user_service.py       #   пользователи и роли
│   │   ├── audit_service.py      #   запись действий в audit_log
│   │   ├── invite_service.py     #   одноразовые ссылки-приглашения
│   │   ├── access_control.py     #   гарды admin_only / super_admin_only
│   │   ├── ocr_service.py        #   OCR и извлечение моделей из текста
│   │   ├── image_preprocessing.py #  подготовка изображения (OpenCV)
│   │   ├── image_service.py      #   сохранение фото товара
│   │   └── temp_file_service.py  #   временные фото из поиска (TTL 1 час)
│   │   # voice_service.py, estimate_service.py, handlers/search.py — legacy,
│   │   # пока не подключены в bot.py
│   ├── states/                   # FSM-состояния aiogram
│   └── utils/                    # нормализация, парсинг, форматирование
├── data/                         # загруженные файлы и изображения (gitignored)
├── temp/photos/                  # временные фото из поиска (gitignored)
├── docker-compose.yml            # PostgreSQL
├── requirements.txt
├── .env.example
└── README.md
```

> Весь Python-код собран в `src/`. Команды запускаются **из корня проекта**,
> чтобы относительные пути к `data/` и `temp/` разрешались корректно.

---

## 🚀 Быстрый старт

```bash
# 1. Зависимости
python3 -m venv .venv
.venv/bin/pip install -r requirements.txt

# 2. Конфигурация
cp .env.example .env        # впишите BOT_TOKEN и свой ADMIN_IDS

# 3. База данных
docker compose up -d postgres

# 4. Запуск бота
.venv/bin/python src/bot.py
```

PostgreSQL публикуется на `localhost:5433`, чтобы не конфликтовать с локальным
PostgreSQL на `5432`. Проверить состояние контейнера (должен быть `healthy`):

```bash
docker compose ps
```

При старте бот создаёт недостающие таблицы и индексы, выставляет таймзону БД
и подчищает старые временные фото.

---

## ⚙️ Конфигурация (`.env`)

```env
BOT_TOKEN=change-me
DATABASE_URL=postgresql+asyncpg://price:price@localhost:5433/price
LOG_LEVEL=INFO
TIMEZONE=Asia/Samarkand
ADMIN_IDS=
```

| Переменная     | Назначение                                                                       |
| -------------- | -------------------------------------------------------------------------------- |
| `BOT_TOKEN`    | Токен бота из [@BotFather](https://t.me/BotFather). Читается **только** из `.env`. |
| `DATABASE_URL` | Строка подключения asyncpg к PostgreSQL.                                          |
| `LOG_LEVEL`    | Уровень логирования (`INFO`, `DEBUG`, …).                                         |
| `TIMEZONE`     | Таймзона для меток времени (контейнер Postgres по умолчанию в UTC).               |
| `ADMIN_IDS`    | Telegram id через запятую — стартовые **супер-админы**. Дальше роли — через бота. |

`ADMIN_IDS` только «засеивает» первого владельца: указанные id при первом
сообщении становятся `SUPER_ADMIN`. Все остальные проверки прав идут по таблице
`users`. Свой id можно узнать у [@userinfobot](https://t.me/userinfobot).

---

## 👮 Роли и управление доступом

Проверки доступа выполняются в middleware и гардами **до** бизнес-логики, а роль
читается из таблицы `users`.

| Роль          | Права                                                                 |
| ------------- | --------------------------------------------------------------------- |
| `SUPER_ADMIN` | всё: добавление/удаление пользователей и админов + права `ADMIN`.     |
| `ADMIN`       | импорт Excel, добавление / обновление / удаление товаров, поиск.      |
| `USER`        | только поиск и просмотр.                                              |

- При **первом сообщении** пользователь автоматически заводится в `users` с ролью
  `USER` (имя = Telegram `first_name`/`username`). Дубликаты исключены.
- **Добавление пользователя** (только `SUPER_ADMIN`): кнопка «👤 Добавить
  пользователя» → выбор роли → одноразовая ссылка `https://t.me/<bot>?start=<token>`.
  Получатель переходит по ней и получает выбранную роль. Ссылка **одноразовая** и
  никогда не понижает права того, кто уже выше (супер-админ по своей ссылке не
  «слетает»).
- **Удаление пользователя** (только `SUPER_ADMIN`): кнопка «🚫 Удалить
  пользователя» → выбор роли → список → подтверждение Да/Нет. Кнопка «⬅️ Назад»
  возвращает на предыдущий шаг.
- Повысить роль вручную можно и в БД:
  `UPDATE users SET role='ADMIN' WHERE telegram_id=<id>;`

---

## 🗃️ Модель данных

**`products`**

| Поле               | Описание                                                       |
| ------------------ | -------------------------------------------------------------- |
| `id`               | Первичный ключ.                                                |
| `model`            | Исходное наименование/модель из прайса.                        |
| `normalized_model` | Нормализованная модель (уникальный ключ поиска).               |
| `description`      | Описание / характеристики (необязательно).                     |
| `price`            | Цена.                                                          |
| `stock`            | Остаток (если был в Excel, иначе `не указан`).                 |
| `image_path`       | Путь к файлу или URL изображения.                              |
| `created_at`       | Дата создания записи.                                          |

**`users`** — `id`, `telegram_id` (UNIQUE), `name`, `role`, `created_at`.

**`audit_log`** — `id`, `user_id` (FK), `telegram_id`, `action`, `details`,
`created_at`. Индексы по `user_id`, `created_at`, `action`.

**`invites`** — `id`, `token` (UNIQUE), `role`, `created_by`, `used_by_telegram_id`,
`used_at`, `created_at`. Одноразовые ссылки-приглашения.

`normalized_model` строится из `model`: строка переводится в верхний регистр,
а пробелы, дефисы и прочие не буквенно-цифровые символы удаляются:

```text
DS-7108NI-Q1  →  DS7108NIQ1
```

Для `normalized_model` созданы два индекса:

- btree `ix_products_normalized_model` — точные совпадения;
- trigram GIN `ix_products_normalized_model_trgm` — ускорение `ILIKE '%...%'`.

---

## 🧾 Аудит

В `audit_log` пишутся только **изменения данных** и отказы в доступе (поиск и
входы не логируются). Каждая запись содержит `telegram_id`, `user_id`, `action`,
`details`, `created_at`.

| Действие         | Когда                                          |
| ---------------- | ---------------------------------------------- |
| `ADD_PRODUCT`    | добавлен товар                                 |
| `UPDATE_PRODUCT` | обновлён товар                                 |
| `DELETE_PRODUCT` | удалён товар                                   |
| `IMPORT_EXCEL`   | импортирован прайс-лист                         |
| `ACCESS_DENIED`  | попытка действия без прав                        |
| `ERROR`          | сбой при импорте Excel                          |

---

## 📊 Импорт Excel

### Из командной строки

```bash
.venv/bin/python src/import_excel.py "data/DARIAN HIKVISION PRICE (8).xlsx"

# пересоздать таблицу products перед импортом:
.venv/bin/python src/import_excel.py "data/DARIAN HIKVISION PRICE (8).xlsx" --recreate
```

### Через Telegram

1. Нажмите **«Загрузить Excel»** (доступно `ADMIN` и `SUPER_ADMIN`).
2. Отправьте файл документом в формате **`.xlsx`** (другие форматы отклоняются).
3. Бот импортирует товары и обновит существующие позиции по `normalized_model`.

### Формат файла

Поддерживаемые названия колонок:

| Поле      | Варианты заголовка                                              |
| --------- | -------------------------------------------------------------- |
| Модель    | `Наименование`, `Модель`, `Model`, `IMOU Model`, `Артикул`, `SKU`, `Код` |
| Фото      | `Изображение`, `Фото`, `Photo`, `Image`, `Image Path`, `Картинка` |
| Описание  | `Описание`, `Description`, `Desc`, `Характеристики`            |
| Цена      | `Цена`, `Price`, `Стоимость`, `Прайс`                          |

- Если ячейка `Изображение` пустая → в БД пишется `NULL`.
- Если картинка вставлена в ячейку как embedded image, импорт извлекает её
  в `data/product_images/` и сохраняет путь в `image_path`.
- Импорт умеет читать файлы, где строка заголовков идёт не первой.

---

## 🔍 Поиск

| Способ      | Как работает                                                              |
| ----------- | ------------------------------------------------------------------------- |
| **Текст**   | Частичное совпадение по `normalized_model` (`ILIKE '%7108%'`), затем fuzzy. |
| **Фото**    | Скачивание во `temp/photos/` → препроцессинг (OpenCV) → EasyOCR → выделение и нормализация моделей → exact, затем fuzzy через `rapidfuzz`. |

Пример: запрос `7108` находит `DS-7108NI-Q1`. Если найдено несколько товаров,
бот отправит каждый результат отдельным сообщением с моделью, ценой, остатком,
описанием и фото (если оно есть). Если по фото ничего не нашлось, бот покажет
распознанную модель и сообщит, что товар не найден.

Фото из поиска складываются в `temp/photos/`, живут **1 час** и затем удаляются
фоновой задачей (`temp_file_service`). Изображение товара берётся из колонки
`Изображение` и может быть URL, локальным путём или пустым.

---

## 🧠 OCR

OCR-зависимости: `easyocr`, `opencv-python`, `Pillow`, `rapidfuzz`,
а также CPU-сборки `torch` и `torchvision`. В `requirements.txt` указан
PyTorch CPU index — чтобы не тянуть CUDA-пакеты на сервер без GPU.

Бот распознаёт модели вида `DS-2CD2043G2-I`, `DHI-XVR5108HS-I3`,
`IPC-A22EP`, `DH-IPC-HDW1230T1`.
